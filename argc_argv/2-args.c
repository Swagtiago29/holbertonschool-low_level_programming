#include <stdio.h>
#include "main.h"

/**
* main - Entry point
*
* Return: Always 0.
* @argc: int #strings
* @argv: array de strings
*/
int main(int argc __attribute__((unused)), char *argv[])
{
	int i = 0;
	
	for (i >= 0; *argv[i] == '\0'; i++)
		(printf("%s\n", argv[i]);
	return (0);
