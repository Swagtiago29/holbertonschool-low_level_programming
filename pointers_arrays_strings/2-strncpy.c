#include "main.h"

/**
* char *_strncpy - Entry point of my code
*
* Return: Always 0.
* @n: jua n
*/
char *_strncpy(char *dest, char *src, int n)
{
	int o = 0, p = 0, l = 0;

	for (; 
	for (; p <= n - ; p++)
	{
		l++
		dest[o++] = src[p];
	}
	return (dest);
}
