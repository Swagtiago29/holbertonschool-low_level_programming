#include <stdio.h>
#include "lists.h"
#include "main.h"
#include <stdlib.h>

/**
 * print_list - listita
 * Return: number of nodes
 * @h: hhhhhhh
 */
list_t *add_node(list_t **head, const char *str)
{

	list_t liston

	while (head != NULL)
	{
		i++;
		if (h->str == NULL)
		{
			printf("[0] (nil)\n");
			h = h->next;
		}
		else
		{
			printf("[%u] %s\n", h->len, h->str);
			h = h->next;
		}
	}
	return (i);
}
