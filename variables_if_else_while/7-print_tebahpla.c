#include <stdio.h>

/**
 * main - Entry Point
 * Return: Always 0
 * Description: this is a description
 */
int main(void)
{
	char c;

	for (c = 122; c >= 97; c--)
	{
	putchar(c);
	}
	putchar('\n');
	return (0);
}
