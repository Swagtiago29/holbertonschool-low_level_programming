#include <stdio.h>
#include "variadic_functions.h"
#include <stdlib.h>

/**
* sum_them_all - Entry point
*
* Return: pp
* @n: number of args
*/
void print_all(const char * const format, ...);
{
	int i = 0;
	int l = 0;

	while (format[l] != '\0')
		{
		l++;
		
		}
}
